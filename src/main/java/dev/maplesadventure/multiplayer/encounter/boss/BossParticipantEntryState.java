package dev.maplesadventure.multiplayer.encounter.boss;

/** Persisted per-attempt fog-gate participation; geometry is deliberately not a room authority. */
public enum BossParticipantEntryState {
    OUTSIDE,
    ENTERED
}
