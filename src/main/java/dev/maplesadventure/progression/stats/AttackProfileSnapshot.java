package dev.maplesadventure.progression.stats;

/** Deliberately unavailable until weapon requirements/scaling have a real implementation. */
public record AttackProfileSnapshot(CharacterStatValue mainHand, CharacterStatValue offHand) {}
