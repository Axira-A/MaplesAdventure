package dev.maplesadventure.progression.upgrade;

/** Neutral origins for one reusable level-up workflow. Only BONFIRE and ADMIN issue access today. */
public enum UpgradeAccessType {
    BONFIRE,
    NPC,
    ADMIN,
    SCRIPT
}
